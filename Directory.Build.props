<Project>

  <PropertyGroup>
    <!-- The product version, in one place. /__admin/health reports it, so it must be bumped with the
         release (alongside ui/package.json) rather than left to drift. -->
    <Version>1.2.0</Version>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <EnforceCodeStyleInBuild>false</EnforceCodeStyleInBuild>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!-- Licensing metadata (Apache-2.0). Applied to any project that later opts into packaging. -->
  <PropertyGroup>
    <Authors>Ömer Çelik</Authors>
    <Copyright>Copyright 2026 Ömer Çelik</Copyright>
    <PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>
  </PropertyGroup>

</Project>
